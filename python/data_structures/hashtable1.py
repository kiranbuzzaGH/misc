"""Implementation of a hash table using chaining to avoid collisions"""

class HashTable:
    #TODO: create hash function
    array = []

    def insert(array, x):
        #TODO: prepend data x to linked list at the hashed location

    def search(array, key):
        #TODO: return the result of a list search for key at the hashed location

    def delete(array, x):
        #TODO: list delete value x at the hashed location
